import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function Page() {
  return <ScreenPlaceholder title="Категории" status="Каркас собран, содержимое экрана — следующим шагом." />;
}
